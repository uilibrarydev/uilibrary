import type { TSVGIconSize } from '../SVGIcons/types'

export const FRAME_SIZE_MAPPING: { [key: string]: TSVGIconSize } = {
  large: 'large',
  medium: 'medium',
  small: 'small',
}
export const ICON_SIZE_MAPPING: { [key: string]: TSVGIconSize } = {
  large: 'xxlarge',
  medium: 'xlarge',
  small: 'medium',
}
