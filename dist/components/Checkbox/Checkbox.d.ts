import { ForwardedRef, ReactElement } from 'react';
import { TCheckboxProps } from './types';
export declare const Checkbox: (props: TCheckboxProps, ref: ForwardedRef<HTMLInputElement>) => ReactElement | null;
