import { ReactElement } from 'react';
import { ISVGIconProps } from './types';
export declare const IconQuestionFilled: ({ size, type, className, onClick, refHandler, id, dataId }: ISVGIconProps) => ReactElement;
export default IconQuestionFilled;
