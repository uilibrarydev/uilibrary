import { ReactElement } from 'react';
import { TNestedMenuProps } from './types';
export declare const NestedMenu: ({ menuItems, parentRef, onClose, isOpen, position, className }: TNestedMenuProps) => ReactElement | null;
